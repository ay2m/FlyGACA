# The Capacitor plugin set (v6 plugins on a v8 core) predates strict peer
# resolution; package-lock.json was generated with legacy peer deps. Without
# this flag every plain `npm ci` (GitHub Actions, Vercel) fails on ERESOLVE.
legacy-peer-deps=true

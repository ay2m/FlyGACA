/**
 * Hand-written declarations for the plain-ESM Xcode-localization script, typing
 * only the pure mutator the Vitest suite imports. Keep in sync with
 * ios-localize.mjs.
 */
export function mutatePbxproj(plist: string, uuidFn?: () => string): string;

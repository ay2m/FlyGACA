# Same reason as the root .npmrc: typescript-eslint@8's `typescript` peer range
# excludes the TS 7 this package compiles with, so strict resolution ERESOLVEs.
# Drop together with the root flag once typescript-eslint supports TS 7.
legacy-peer-deps=true
